# Free uptime monitor: pings HF Spaces /health every 10 minutes.
# Currently manual-only — re-enable the cron schedule below once the
# Space at https://agwgwa-smiles2iupac.hf.space is built and serving.
# Failures show up in the Actions tab as a free uptime monitor.
name: Healthcheck

on:
  workflow_dispatch:  # manual only until the Space exists
  # schedule:
  #   - cron: "*/10 * * * *"

jobs:
  ping:
    name: GET /health
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Ping HF Space
        run: |
          set -euo pipefail
          URL="https://agwgwa-smiles2iupac.hf.space/health"
          # -f fails on non-2xx; -sS hides progress but shows errors; --max-time guards against hangs
          response=$(curl -fsS --max-time 30 "$URL")
          echo "Response: $response"
          # Hard-fail unless body contains the expected status field.
          echo "$response" | grep -q '"status"[[:space:]]*:[[:space:]]*"ok"' \
            || { echo "Health endpoint did not return status=ok"; exit 1; }
